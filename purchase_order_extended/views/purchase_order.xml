<?xml version="1.0" encoding="utf-8"?>
<odoo>


    <record id="ekara_purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@id='bid_confirm']" position="attributes">
                <attribute name="invisible">
                    state != 'sent' or approval_state != 'Not Applicable'
                </attribute>
            </xpath>
            <xpath expr="//button[@id='draft_confirm']" position="attributes">
                <attribute name="invisible">
                    state != 'draft' or approval_state != 'Not Applicable'
                </attribute>
            </xpath>
            <xpath expr="//button[@name='button_approve']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//button[@name='button_draft']" position="attributes">
                <attribute name="invisible">
                    state != 'cancel' or x_has_request_approval
                </attribute>
            </xpath>
            <xpath expr="//header/field[@name='state']" position="after">
                <button name="button_send_for_approval" type="object" string="Send for a Min Quotes Revision" invisible="quote_matrix_approval_state in ('draft','approved','to_tax_entity_head') or not x_has_request_approval or state not in ('draft','sent','to approve')"/>
            </xpath>
            <xpath expr="//sheet" position="inside">

                <field name="quote_matrix_approval_state" invisible="1"/>

                <div name="quote_matrix_approval_state"
                     class="float-end fs-6 badge rounded-pill fw-bold text-bg-info"
                     invisible="quote_matrix_approval_state != 'to_department_head'">
                    Awaiting Department Head for a Min Quotes Revision
                </div>

                <div name="quote_matrix_approval_state"
                     class="float-end fs-6 badge rounded-pill fw-bold text-bg-info"
                     invisible="quote_matrix_approval_state != 'to_account_head'">
                    Awaiting Account Head for a Min Quotes Revision
                </div>

                <div name="quote_matrix_approval_state"
                     class="float-end fs-6 badge rounded-pill fw-bold text-bg-info"
                     invisible="quote_matrix_approval_state != 'to_tax_entity_head'">
                    Awaiting Tax Entity Head (Director) for a Min Quotes Revision
                </div>
            </xpath>
            <xpath expr="//field[@name='partner_id']" position="attributes">

                <attribute name="context">
                    {
                        'res_partner_search_mode': 'supplier',
                        'show_vat': True,
                        'is_vendor': True
                    }
                </attribute>

                <attribute name="domain">
                    [('state','=','approve')]
                </attribute>

                <attribute name="options">
                    {
                        'no_open': True,
                        'no_quick_create': True,
                        'no_create': True
                    }
                </attribute>

            </xpath>
            <xpath expr="//field[@name='partner_ref']" position="attributes">
                <attribute name="readonly">
                    state not in ['draft']
                </attribute>
            </xpath>

            <xpath expr="//div[@name='reminder']" position="after">
                <field name="budget_id" required="1" domain="[('budget_analytic_id.state','=','done')]" context="{'search_default_filter_field': True}" readonly="state not in ['draft']" options="{
                           'no_open': True,
                           'no_quick_create': True,
                           'no_create': True
                       }"/>

                <field name="purchase_type"
                       readonly="state not in ['draft']"
                       options="{
                           'no_open': True,
                           'no_quick_create': True,
                           'no_create': True
                       }"/>
                <field name="approval_state" readonly="1"/>

                <field name="x_has_request_approval" invisible="1"/>

                <field name="x_review_result" invisible="1"/>

            </xpath>
            <xpath expr="//page[@name='products']//field[@name='order_line']/list//field[@name='product_id']" position="attributes">
                <attribute name="options">
                    {
                        'no_open': True,
                        'no_quick_create': True,
                        'no_create': True
                    }
                </attribute>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page string="Approval Reason" name="approval_reason" invisible="quote_matrix_approval_state in 'draft'">
                    <field name="approval_history"/>
                </page>
            </xpath>
        </field>
    </record>

    <record id="purchase_requisition.purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="replace">
                <field name="partner_id"
                    widget="res_partner_many2one"
                    context="{'res_partner_search_mode': 'supplier', 'show_vat': True, 'is_vendor': True}"
                    placeholder="Name, TIN, Email, or Reference"
                    readonly="state in ['purchase', 'done', 'cancel']"
                    options="{'no_open': True, 'no_quick_create':True,'no_create': True}" 
                    domain="[('state','=','approve')]" force_save="1"/>
            </field>
            <field name="partner_ref" position="after">
                <field name="requisition_id" domain="[('state', 'in', ('in_progress', 'open', 'ongoing')), ('vendor_id', 'in', (partner_id, False)), ('company_id', '=', company_id)]"
                options="{'no_create': True}"/>
            </field>
            <xpath expr="//page[@name='purchase_delivery_invoice']" position="after">
                <page string="Alternatives" name="alternative_pos">
                    <group>
                        <group>
                            <p colspan="2">Create a call for tender by adding alternative requests for quotation to different vendors.
                            Make your choice by selecting the best combination of lead time, OTD and/or total amount.
                            By comparing product lines you can also decide to order some products from one vendor and others from another vendor.</p>
                        </group>
                        <group>
                            <p colspan="2">
                                <button name="action_create_alternative" type="object" class="btn-link d-block" string="Create Alternative" icon="fa-copy"/>
                                <button name="action_compare_alternative_lines" type="object" class="btn-link d-block" string="Compare Product Lines" icon="fa-bar-chart" invisible="not alternative_po_ids"/>
                            </p>
                        </group>
                    </group>
                    <field name="alternative_po_ids" readonly="not id" widget="many2many_alt_pos" context="{'quotation_only': True}">
                        <list string="Alternative Purchase Order" decoration-muted="state in ['cancel', 'purchase', 'done']" decoration-bf="id == parent.id">
                            <control>
                                <create string="Link to Existing RfQ"/>
                            </control>
                            <field name="currency_id" column_invisible="1"/>
                            <field name="partner_id" readonly="state in ['cancel', 'done', 'purchase']"/>
                            <field name="name" string="Reference"/>
                            <field name="date_planned"/>
                            <field name="amount_total"/>
                            <field name="state"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>


</odoo>
